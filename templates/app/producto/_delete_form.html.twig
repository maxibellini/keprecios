  <a class="btn btn-link btn-sm" data-toggle="collapse" href="#collapseExample2" role="button" aria-expanded="false" aria-controls="collapseExample2">
    Quiero dar de baja el producto
  </a>

<div class="collapse" id="collapseExample2">
  <div class="card card-body">
<form method="post" action="{{ path('app_producto_baja', {'id': producto.id}) }}" onsubmit="return confirm('¿Esta seguro de que quiere dar de baja el producto?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
    <button class="btn btn-sm" style="background-color: red; color:white">🗑️ Dar de baja el producto</button>
</form>


  </div>
</div>